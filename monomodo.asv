function calcularBL = monomodo(dispersion, lambda, B, L)
clc;

if dispersion == 0 && lambda == 0
    calcularBL = 'No se puede calcular e'

if B == 0 && L == 0
    BL = 1/(dispersion*lambda);
end
calcularBL = BL;